# Task: Goethe B1 Hören, Teil 2 (items 11–15)

Write one organised monologue (heard once) and five a/b/c items.

Blueprint (binding):

```json
$blueprint
```

- One speaker (guide, organiser, trainer). `gender` equals `speaker_gender`.
- Setting: the blueprint's `setting`; invent concrete names, times and places.
- 450–600 words, as 6–9 turns by the same speaker, roughly one turn per information block, each turn at most 80 words.
- Structure: short welcome, then five information blocks in order; item 11 is answered in block 1, item 15 in block 5. Filler blocks between them are fine.
- Each block holds two or three competing facts of the same class (times, places, statuses, conditions), so all three options of its item occur in the audio.
- Stems are short questions ("Wo trifft sich die Gruppe am Nachmittag?"). Options are short, parallel and of one semantic class.

Previously used skins to avoid: $avoid
