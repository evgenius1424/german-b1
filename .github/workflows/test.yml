name: Test

on:
  push:
  pull_request:

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=scripts python -m unittest discover -s tests -v
